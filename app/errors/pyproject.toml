[project]
name = "vt-errors"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["vt-logger"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/errors"]

[tool.uv.sources]
vt-logger = { workspace = true }
